# LeanAI — Requirements
# Install: pip install -r requirements.txt

# Core — LLM inference (LOCAL models only; skip if you only use remote
# endpoints — see requirements-remote.txt and REMOTE_MODELS.md)
llama-cpp-python>=0.3.0

# Remote endpoints (OpenAI-compatible / Ollama) config file parsing
pyyaml>=6.0

# Model download
huggingface-hub>=0.20.0

# Vector memory (M6 InfiniteContext + memory store)
chromadb>=0.4.0

# Embeddings for semantic search (M6) and conversation memory
sentence-transformers>=2.2.0

# Hybrid retrieval — BM25 keyword scoring (M6 InfiniteContext)
# Falls back to a built-in manual implementation if not installed,
# but the library is faster and more accurate.
rank-bm25>=0.2.2

# File watcher (M9 Watchguard — real-time brain + graph updates)
# Uses ReadDirectoryChangesW on Windows, inotify on Linux, FSEvents on macOS.
# Without this, /watchguard start fails gracefully and LeanAI works fine
# without M9 — only Watchguard itself is unavailable.
watchdog>=3.0

# Web server
fastapi>=0.100.0
uvicorn>=0.23.0

# Data/ML utilities
numpy>=1.24.0
pydantic>=2.0.0

# Symbolic verification (optional — Z3 math/algebra proofs)
z3-solver>=4.12.0
sympy>=1.12

# Testing
pytest>=7.0.0
psutil>=5.9.0
